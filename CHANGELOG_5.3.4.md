# Changelog 5.3.4

- Menambahkan field `frequency` ke query Meta Insights untuk semua mode.
- Menambahkan kolom `Frekuensi` ke mode rinci XLSX.
- Menambahkan kolom `Frekuensi` ke CSV utama, sehingga tersedia untuk mode rekap maupun rinci.
- Mode rekap tetap menggunakan nilai `frequency` Meta seperti versi sebelumnya.
- Logic `Hasil` tetap strict pada `actions:onsite_conversion.messaging_conversation_started_7d`.
- Tidak ada perubahan pada input `ID ACCOUNT | TOKEN | CABANG | BISNIS`.
